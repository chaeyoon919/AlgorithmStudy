# 점검이 필요한 자전거 찾기
## 🧠 해결 전략

1. `WHERE` 을 이용하여 2021년 1월 대여된 자전거만 뽑아내기
2. `GROUP BY` 를 사용하여 자전거 별로 주행거리 합산 집계
3. `HAVING` 을 사용하여 주행거리 50km 이상 조건 부여

## 🧾 SQL 풀이

```sql
SELECT
  bike_id
  -- ,SUM(distance)
FROM rental_history
WHERE DATE(rent_at)>="2021-01-01" AND DATE(rent_at)<"2021-02-01"
GROUP BY bike_id
HAVING SUM(distance) >= 50000
-- LIMIT 50

-- 1km = 1000m
-- 50km = 50000m
```

## ✅ 개념 정리

1. 문제를 잘 읽자
    1. 처음에 50km 이상에서 50만 보고, `HAVING SUM(distance) >= 50`  로 품
